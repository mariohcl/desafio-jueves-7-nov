<div class="container-menu">
  <div class="container-fluid container-nav-custom">

    <nav class="navbar navbar-expand-lg navbar-light navbar-custom">
        <a class="navbar-brand brand-custom" href="<%= root_path %>">Tasks</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div id="navbarNavDropdown">
            <ul class="navbar-nav mr-auto">

              <% if user_signed_in? %>


              <li class="nav-item">
                <a href="#" class="name">
                  <%= current_user.email %>
                </a>

              </li>

              <li class="nav-item"><a href="<%= destroy_user_session_path %>" class="salir" data-method="DELETE">Salir</a></li>

              <% else %>
              <li class="nav-item"><a href="<%= new_user_session_path %>">Ingreso</a></li>
              <li class="nav-item"><a href="<%= new_user_registration_path %>" class="reg">Registro</a></li>
              <% end %>
            </ul>
            <ul class="navbar-nav navbar-nav-custom">


            </ul>


        </div>

        <% if user_signed_in? %>

        <p>Tareas completadas <%= current_user.taskcompleteds.count %> / <%= Task.all.count %></p>

        <% end %>

    </nav>
  </div>
</div>
